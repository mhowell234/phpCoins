-- Start of using ForeignCoinDB
USE ForeignCoinDB;

-- Start of using ForeignCoinDB
USE ForeignCoinDB;

-- Start of using ForeignCoinDB
USE ForeignCoinDB;

INSERT INTO MintDate VALUES(1, 4, 1771, 0);

-- Start of using ForeignCoinDB
USE ForeignCoinDB;

-- Start of using ForeignCoinDB
USE ForeignCoinDB;

INSERT INTO MintDate VALUES(2, 5, 886, 0);

-- Start of using ForeignCoinDB
USE ForeignCoinDB;

INSERT INTO MintDate VALUES(3, 7, 1850, 2003);

INSERT INTO MintDate VALUES(4, 8, 1911, 1970);

INSERT INTO MintDate VALUES(5, 9, 886, 0);

-- Start of using ForeignCoinDB
USE ForeignCoinDB;

